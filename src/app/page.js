import Counter from "@/components/Counter/Counter";

const HomePage = () => {
  return (
    <div className="text-4xl text-center font-black flex gap-4 items-center justify-center h-screen">
      <Counter />
    </div>
  );
};

export default HomePage;
